// import { TimeSlots } from "../../helpers";

// describe("Should memoize the day's timeslots split into 15 minute increments", () => {
//   test("Should calculate an entire day's timeslots split by", () => {
//     let timeslot = new TimeSlots();
//     timeslot.calculate();
//     expect(timeslot.times).not.toHaveLength(0);
//     expect(timeslot.isCalculated).toBe(true);
//   });
// });
